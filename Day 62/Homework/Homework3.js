let names = `Banana`;

let index = 0;
let count = 0;

while(index < names.length){
    if(names[index] === `a`){
     count ++;
    }
    index++;
}

console.log(`Letter A was repeted`, count ,`times!`);

