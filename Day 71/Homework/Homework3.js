/*
super() ფუნქცია:
- გამოიყენება კლასებში მემკვიდრეობისას (`extends`).
- მშობელი class-ის constructor-ის გამოძახებას ახდენს.
- super() უნდა გამოიძახო constructor-ის შიგნით, სანამ this-ს გამოიყენებ.
- ასევე შეიძლება super-ის გამოყენება მშობელი class-ის მეთოდების გამოსაძახებად.



localStorage:
- არის ბრაუზერის შიდა მეხსიერება, სადაც შეგიძლია შეინახო მონაცემები "key-value" წყვილებით.
- მონაცემები არ იშლება ბრაუზერის დახურვის შემდეგ (persistent storage).
- მონაცემები ინახება ტექსტის სახით (string), ამიტომ ობიექტების შესანახად უნდა გამოიყენო JSON.stringify().
- წასაკითხად გამოიყენე JSON.parse().

მეთოდები:
- localStorage.setItem("key", "value")  // მონაცემის შენახვა
- localStorage.getItem("key")           // მონაცემის წაკითხვა
- localStorage.removeItem("key")        // კონკრეტული მონაცემის წაშლა
- localStorage.clear()                  // ყველა მონაცემის წაშლა
*/
